<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ScoringSystem.Mobile.Views.LoginPage">
    <ContentPage.ToolbarItems>

    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Login In"
                   FontSize="Large"
                   />

            <Entry x:Name="UserEmail" Text="{Binding Login.Email}" Placeholder="Login"  Keyboard="Email"/>
            <Entry x:Name="UserPassword" Text="{Binding Login.Password}" Placeholder="Password" IsPassword="True"/>
            <Button HorizontalOptions="Center" Text="Log In" Command="{Binding LoginCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>